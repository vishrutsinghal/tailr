# Reference Project Boundary

The public fixtures describe a small, sanitized Python order-service shape:
validation, service orchestration, an API boundary, a dependency manifest, and
focused tests. It is deliberately an artifact contract rather than copied
customer code. Real model-run contributors must use only code they are allowed
to disclose and must submit only sanitized derived artifacts to the recorder.
