# TailTrail Interactive Plan Artifact

Explain the plan from saved planning evidence and preserve the same run ID. A
bounded investigation needs explicit read-only approval and may read only
planned paths.

A material update creates a versioned revision and requires separate approval;
stale approval blocked protects the reviewed boundary. rejection preserves the run and may route to AIDLC requirements.

Intent Bridge source-owned requirements are not rewritten locally. Full work
routes to official AIDLC when that authority owns refinement. A deferred harness
is shown with its activation condition. The token estimate is not measured model
usage.
