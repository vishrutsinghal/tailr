# Deferred Phase 9 Workflow Plan

1. Define closed CI policy, receipt, and continuation-index schemas.
2. Implement exact linkage/freshness/provenance checks and deterministic receipt ordering.
3. Advance only canonical metadata transitions for eligible policy stages.
4. Add CLI and MCP status/ingest surfaces with explicit approval and safe-reference validation.
5. Synchronize installer, registry, command documentation, roadmap, and all three generated host surfaces.
6. Prove positive advancement and negative authority, privacy, freshness, ordering, failure, cancellation, recovery, and closure boundaries.

Validation gate: focused Phase 9 tests, Phase 7–9/MCP/host/installer/registry
regression tests, strict registry validation, MCP doctor, host conformance,
Python compilation, diff checks, and the repository-wide unittest suite.
