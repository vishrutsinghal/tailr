# Local TailTrail State

This folder is where TailTrail-generated local state may appear during the demo.

Examples:

- `code-graph-cache.json`
- `learning-events.jsonl`
- `outcome-events.jsonl`
- `value-report.md`

Do not assume these files exist before the presenter approves the relevant TailTrail command.

